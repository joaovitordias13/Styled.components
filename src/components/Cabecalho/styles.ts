import style from 'styled-components'

export const Cabecalho = style.header`
  background-color: var(--cor-secundaria);
  color: var(--cor-principal);
  text-align: center;
  padding: 24px 0;
`

export const Titulo = style.h1`
  font-size:16px
`
